<!-- 하네스 자기개선 루프의 루프별 히스토리. 형식: 루프번호 / 날짜 / 바뀐 것 / 근거. 항목 내용 추후. -->

- 루프14 / 2026-07-21 / (A=분모 기수 최신화[§1 blocker·비용0], B=국면 보존[§1 blocker], C=비용 최적화) 파일 무겹침 3덩어리 순차 자작. ■ A(src/sources/dart.py): 재무 분모 staleness 해소. `_YEARS=["2024","2023","2022"]` 하드코딩 제거 → `_years()`로 현재 연도부터 역순 동적 계산(원칙 1, 미공시 최신연도는 status 013 자동 탈락). 캐시 무효화 신설: 캐시 rows 의 bsns_year 가 최신 후보보다 낡으면 '더 새 연도'만 DART 조회(rcept_no 비교)해 새 보고서면 갱신 — 판정을 rows 자체 메타로 하여 캐시 포맷 불변(구 캐시 동작). ★최신성 확인은 베스트 에포트(키·쿼터·네트워크 막히면 캐시 유지, 크래시 금지 — 결정론 테스트도 캐시로 동작), 인스턴스당 corp 1회(_validated_corps). 3분모 같은 보고서(불변). ★연간 사업보고서(11011)만(반기·분기 금지 사유 docs/magnitude-rules.md 기록). 라이브 실측(DART 무비용): 캐시 5사 전부 FY2024→최신 FY2025('갱신 발생'), CJ FY2025 자산총계 29.74조 = 감사인 제시 '제19기 29.7조' 일치(발단 의혹 해소), end-to-end _statement_rows 가 FY2025 반환·기록 확인 후 픽스처 원복. ■ B(src/judge/prescreen.py): 국면 오병합 해소. 루프11 D30 B의 "검토/확정 단계도 같은 event" 조항 반전 → 병합 단위를 '같은 사안'에서 '같은 사건의 같은 국면'으로 좁힘(프롬프트만, 군집 코드 불변). 같은 발표를 매체가 다르게 쓴 중복만 병합→국면별 대표 1건 통과(같은 국면 다매체는 계속 접힘), 국면(진전·쟁점·확정성) 다르면 다른 event_id. ★고정 단계표 없이 원리 서술(원칙 1), 부산/울산 가드 유지, 표시층 event_group(루프13) 불침범(입구=국면중복제거·표시층=사안묶기 역할 분리). 라이브 실측(Haiku): RED(구) survivors 3·과징금확정 '중복' 접힘(통과 False) → GREEN(신) survivors 6·4개 법적국면 전부 통과(과징금확정 True), 정책검토만 관련성 관문서 접힘(보존), 대조군 부산화재·세무조사 양쪽 통과(가드 무손상). ■ C(schema.py·prompts/system.txt·engine.py + tools/cost_meter.py): [C-1 출력 다이어트] 미표시 서술 4종(relevance_reason·direction_reason·intrinsic_risk_reason·magnitude.reason) 스키마 제거 → 생성 중단. ★근거(evidence·amount_quote·계정 quote)·표시 이유(one_line_reason 등) 유지(줄인 건 설명이지 근거 아님 — 원칙 3·5), dataclass 필드 유지(구 덤프·테스트 하위호환, parse_batch .get 기본값), 표시 이유엔 '간결하게(인용 제외)' 지시 추가. [C-2 캐싱] 판단 system(count_tokens 실측 13,184토큰>Opus 4096) 에 cache_control → 첫 청크 이후 0.1x 읽기(회사·청크 공통 프리픽스). 깔때기 _SYSTEM 1,547토큰(Haiku 4096 미만)이라 조용히 캐시 안 됨(무해·정직 보고). [C-3 계측] tools/cost_meter.py(on_usage 훅→Haiku/Opus 입출력·캐시·USD, 판단 무영향·tools/ 분리). ■ 비용 순증감(★추정, 라이브 전량 실측 필요): B 판단↑ vs C 절감(캐싱 ~$0.4~0.5 + 다이어트 ~$0.3~0.5) > B 증가(~$0.1~0.45) → 회사당 ~$3.4→~$2.5~2.8 순감 예상. ■ 검증: 무API 116 pass 회귀0(스키마 라운드트립 구/신 둘 다 파싱·dart 베스트에포트 probe·cost_meter 산술), 라이브 22건은 빈 키 fail-fast(무과금)라 결정론 제외. 유지: 계정 울타리·BS 섹션/계정 보존(루프11·12)·증발방어·event_group(루프13)·접힘≠삭제·정합·severity_rank·주체성/감사중요 원리. pending: prescreen-must-not-merge-event-phases·total-verify-2026-07-20 #4 에 개발 반영 보고(불변식 신설은 검증방 몫, 원칙 10). decision-log D34.

- 루프13 / 2026-07-20 / (A=폴백 계정 증발[비용0], B=판단 4종[판단 캐시 전량 무효]) A/B 파일 무겹침 — A(src/sources/dart.py)는 내 루프12 맥락 상속한 fork 로 병렬, B(prompts·schema·serialize·index.html)는 순차 자작. ■ A 폴백 행 보존: _group_by_statement 의 account_nm dedup 제거 → 옛 예시 덤프를 /api/example 로 열 때 발동하는 폴백표에서도 동명 계정 드롭 0. ★_distinct_accounts(판단 울타리) 절대 미변경(fork grep 로 확인) — 그건 LLM 프롬프트 문자열·판단 캐시 키. 실측 5사 폴백표 BS 원본이름행==표시: 한화55·삼성52·태영48·CJ52(+1)·현대58(+8) 드롭0(폴백은 grand·소계 포함 raw 라 T계정보다 큼). test_account_statements 갱신 6 pass. ■ B(판단 프롬프트·표시, 캐시 무효 고지). 전부 판별원리 서술로 정밀화(고정표·키워드·임계치 금지, 원칙 1·8): [B-1 주체성 결정성] 같은 서머리·코너 기사가 실행마다 C↔상단 뒤집힌 비결정성 → '보도 목적이 이 회사를 알리는가 vs 여러 대상을 모은 묶음의 한 항목인가' 원리 + '개별 소식 나열형 묶음·코너·서머리의 한 항목'을 타사언급 갈래(b)로 명시, ★매체·코너명 문자열 매칭 금지·애매하면 복수주체, subject_role_reason 을 A/B/C 전부 필수화(추적성). [B-2 감사중요 조임] 표시20중 14건(70%) 남발 → true 에 '세 관문'(구체성-정책검토단계 제외·회사특정성·후속절차 지목가능성) 전부 통과 요구, 정책검토·업계동향·경미·호재/홍보/애널의견 false(호재도 안에 세일앤리스백류 구체거래 있으면 그 거래로 true), ★2300억 세일앤리스백 중립노출은 유지(관문 통과), 임계치·건수상한 없음. [B-3 사건군 도배] 표시20중 9건 담합 하나의 사안 → ★중복병합으로 안 풂(국면 소실=원칙7). schema 에 event_group 신설(같은 사안=같은 라벨·국면 달라도 안 쪼갬, 다른 사안=다른 라벨, 애매하면 빈값=오병합 금지), serialize.severity_list_view 가 라벨로 '대표 1건 최상위+나머지 국면 대표 아래 접기'(삭제 아님). 대표=이미 매겨진 심각도 순서 첫 등장(원칙1·8, 새 공식 없음). index.html 사건군 묶음 UI. shown 카운트=visible 전체(정합 불변). [B-4 자기모순] 스스로 '경미·소액'이라 쓰고 상단 노출 → severity_rank 자기일관성 조항(판단층 정밀화, 표시층 '경미' 문자열 필터 아님, 원칙1; 미상≠작음 유지). ■ 라우팅 이름 모호성 관측 반영: 계정 선택 지점에 '배정은 계정군(이름) 단위 — 유동/비유동 같은 이름은 함께 배정(더 보여 안전, 정밀은 카드 근거)' 안내 한 줄(ID 라우팅 정밀화는 가짜 정밀이라 안 함 — 검증방 판정 합치). ■ 고친 파일: src/sources/dart.py(_group_by_statement — fork), src/judge/schema.py(event_group 필드·스키마 required·parse_batch), src/judge/prompts/system.txt(B-1~4 서술), src/api/serialize.py(severity_list_view 사건군 접기·_judgment_from_dict 하위호환), src/api/index.html(사건군 UI·CSS·라우팅 안내), tests/unit/test_account_statements.py(fork). ★schema·serialize 는 B, dart·test_account_statements 는 A — 무겹침. ■ 정직(원칙4·7): B 는 판단 캐시 전량 무효. LLM 실제 거동(서머리 C 판정·감사중요 비율 하락·담합 9건 한 군 묶임)은 라이브(ANTHROPIC_API_KEY) 필요 — 키 없어 '실행 못 함' 정직 고지(억지 통과 없음). 결정론 부분 전량 실증: 프롬프트 .format 무오류·4블록 삽입, event_group 왕복(파싱·하위호환·덤프), 사건군 접기 정합(14→대표6+접힘8=14·증발0·대표 심각도최상위·군건수 정확), 폴백 5사 드롭0, 전체 126 pass(무API, 단독 실행 17초 — 동시 pytest 다중 실행은 캐시 경합으로 445초까지 느려짐, 학습). ■ 유지: 수집·깔때기·증발방어·울타리·접힘≠삭제·정합·루프11 BS·루프12 계정보존/선택키. severity_rank 는 LLM 종합신호 불변(주체성·감사중요·사건군은 8번째 축 아님). pending: fallback-statement-table-still-evaporates·loop12-routing-name-ambiguity 에 개발방 반영 보고 첨부(검증방 후속: _group_by_statement 행수 불변식·라이브 재검증·사건군 접기 불변식은 검증방 몫, 원칙 10). decision-log D32.

- 루프12 / 2026-07-20 / (계정 데이터·표시 계층만, 판단 프롬프트 불변 — 캐시 무효화 방지) 계정 증발 방어. 검증방 긴급조사 발의(harness/pending/new-invariants/screen-vs-raw-account-conservation.md, 우선순위 높음, 원칙 7 위반) 시공. ■ 결함: _build_bs_table 의 collect()·_subtotal_members 가 account_nm(표시명)으로 중복 제거해, 유동/비유동처럼 표시명이 우연히 같은 '서로 다른 계정'을 조용히 드롭. 실측 현대차 57→49(-8), CJ 51→50(-1: 비유동 계약부채 913억). 드롭 0인 한화·삼성·태영은 표기가 달라 우연히 충돌이 없었을 뿐(공통 결함). 2차 피해: '비유동부채 그룹 8 vs 렌더 7', 이름 충돌로 '비유동' 그룹 버튼이 '유동' 계정 선택. ■ 안정 키 선정: account_id 는 비고유(비표준 계정의 account_id 가 '-표준계정코드 미사용-'로 여럿 동일 — 현대차 3행·CJ 2행)라 단독 키로 못 씀 → BS 원시행의 '행 위치(DART 응답 순서)'를 안정 키로. DART 원본이 준 순서(D22 성격, 원칙 1). ■ 고친 파일: src/sources/dart.py(_subtotal_members 반환을 이름→행 위치로·이름 dedup 제거, _enclosing_subtotal_names 신설[동명 구분용 소속 소계명, _pass1_subtotals 서브트리에서 유도], _build_bs_table.collect 이름 dedup 제거·각 행에 key(행 위치)·account_id·context(동명일 때만 소속 소계명)·members(이름, 검증 불변식·표시용)·member_keys(안정 키, 그룹 토글용) 부착. grand 총계·빈 이름만 의도적 제외), src/api/index.html(체크박스 value=안정 키·data-name·data-context, 동명 context 표기, 그룹 data-members=member_keys, toggleGroup·refreshGroupStates 키 매칭, selectedKeys/selectedRouteNames/selectedDisplay 분리[선택·그룹=키, 라우팅=이름], collapseBsSelection 이름+구분표기, 폴백 체크박스에도 data-name, CSS .bsctx), tests/unit/test_bs_subtotal_members.py(members 이름 중복은 동명이인 계정의 정직한 보존이므로 '고유성'은 member_keys 로 검사하도록 갱신 — 개발방 소유). ■ 라우팅 층 분리: 선택/그룹 토글 무결성은 안정 키로, /api/route 로 보내는 것은 이름(계정군 수준 매칭·정밀은 사람 몫 — account-linkage.md). classify_and_rank·_matches_viewer·판단 울타리(_distinct_accounts)·프롬프트·스키마 전부 불변. ■ 판단 울타리 정합: 울타리는 프롬프트 문자열·판단 캐시 키라 안 건드림(설계자 '판단 프롬프트 일절 금지'). 이름 단위 울타리라 동명 하나 더 넣어도 LLM엔 무의미. 대신 '화면 BS 이름 ⊆ 판단 울타리 이름'을 5사 실측 확인(울타리가 이름 단위로 어떤 계정도 숨기지 않음). ■ 실측(무API 캐시 5사): 원본==화면 드롭 0 전사(현대차 8·CJ 1 복구), 안정 키 고유, 동명 계정 유동/비유동 context 구분 노출, CJ 비유동부채 그룹 8=렌더 8·정확 토글(key45[비유동]만 켜고 key31[유동] 안 켬 — 오염 0), 라우팅 이름 dedup. 프론트 로직은 실제 JS를 그대로 옮겨 캐시 bs_table로 구동해 실증(노드 런타임 없어 동치 시뮬). 서버 serialize 경로(bs_table_from_statements)도 키 실려 나감 확인. 전체 118 pass + 2 xpass(검증방 conservation 불변식이 CJ·현대차에서 xfail→XPASS 로 뒤집힘 — 검증방이 strict 승격 예정). ■ 유지(건드리지 않음): 판단 프롬프트(7축·주체성·감사중요·중복군집), 수집·깔때기, 증발방어(reconcile), 접힘≠삭제, 매각예정 처분집단(D30), _detect_subtotals 산술·_assign_sections 섹션 배정, 라우팅 매칭 로직. ■ pending: screen-vs-raw-account-conservation 처방에 '개발방 반영(드롭 제거) 완료' 상태 덧붙여 검증방에 넘김(strict 승격은 검증방 몫, 원칙 10 — 개발방은 자기 검증 기준을 스스로 안 바꾼다). decision-log D31.

- 루프11 / 2026-07-18 / (BS 섹션 복구 + 감사중요 중립 노출 + 방향 정밀화 + 중복 보강; 3덩어리 A/B/C 파일 무겹침) CJ제일제당 라이브 발견 문제들. 세 덩어리를 개발방이 순차 자작(파일 무겹침이라 병렬 가능하나, A의 bs_table 데이터계약이 index.html 렌더와 결합돼 조율 위해 순차). ■ A(src/sources/dart.py): 재무상태표 섹션 붕괴 [최우선]. 원인 규명(추측 아니라 캐시된 실제 CJ DART 로 실측): CJ BS 표시 순서가 비정형(자산총계→자산→자본총계→자본→grand 부채와자본총계(중간)→부채→부채총계→비유동부채; 자본이 부채보다 먼저, grand 가 중간). (1) grand 총계가 running 누적에 leaf 로 섞임(코드가 grand 를 섹션 라벨링에서만 빼고 누적에선 안 뺌), (2) 부채총계 = 유동부채+비유동부채+'매각예정 처분집단 부채'(중간에 낀 leaf)라 '표시된 소계 합'만 보는 pass2 가 부채총계를 leaf 로 오인 → running 30조로 오염 → 부채 블록 못 닫혀 전부 폴백으로 '자본'에 밀려듦(부채 선택가능 0, 자본 24개에 부채 오염, 2차: 자본총계 그룹버튼이 부채 말단까지 선택). 수정: _BS_SECTION_SUBTOTAL_IDS(DART/IFRS 표준 구간합계 계정ID 집합 — 이름표 아니라 마커, sj_div·앵커 쓰는 것과 동일 성격 D22, 원칙 1 준수) + _bs_totals(is_sub[금액산술] / is_total[산술 OR 구조앵커]). is_total 을 (a) running 누적 제외 (b) 선택불가 (c) members 말단 판정에 사용 → 산술이 놓친 구조총계(부채총계)도 총계로 처리돼 running 안 오염. _assign_sections 폴백 변경: 못 닫은 행을 '인접 섹션에 밀어넣기' 대신 None(구분 미확정) 유지 → _build_bs_table 이 unassigned 바구니로 분리 노출(원칙 4·7 — 부채가 자본에 앉는 것보다 미확정이 정직). index.html: 미확정 섹션 렌더 + 폴백 문구 정정('인접 임시배치'→'구분 미확정 분리'). 실측(무API 캐시): CJ 부채 선택가능 0→18, 자본 순수 7(부채 오염 0), 미확정 0·폴백 False. 나머지 4사 '구조앵커로 새로 잡힌 행' 0개 = is_total==is_sub = 바이트 동일(회귀 0). tests/unit/test_bs_section_disposal_group.py 5건(CJ 회귀 고정 + 미확정 거동 직접단위) 추가, 루프8 test_bs_subtotal_members 4건 계속 초록. pending loop7-bs-fallback #1(폴백 카나리) 반영: CJ 가 그 트리거 케이스 — 고정. ■ B(src/judge/prescreen.py): 중복 군집 보강(프롬프트만, 코드 불변). 증상: 같은 발표를 다른 제목으로 쓴 기사가 별개로 남음(CJ '27개 품목 8% 인상' vs '오뚜기·사조 이어 CJ 햇반 8% 올려'; '가격 저울질' 2건). _SYSTEM event_id 지침 정밀화: 판단 기준을 '같은 실제 사건인가(주체·행위/발표·시점)'로 재서술, '제목 문구 닮음' 아님 — 표현·강조점·함께 언급된 회사가 달라도 같은 사건이면 같은 event_id(같은 사건 예시 추가). 단계(검토/확정) 차이도 같은 사안이면 같은 event(단계는 뒤 모델). ★부산/울산 화재 가드 유지(다른 장소=다른 사건), ★'표현 달라서'는 안 합칠 이유 아님·'실제로 다른 사건이라서'가 이유, ★애매하면 다른 정수(오병합=증발 금지, 원칙 7·1). 코드·군집 로직·문자열 임계 없음. ■ C(판단·표시): [C-1 감사중요 중립 노출] 방향과 독립된 audit_attention(예/아니오) 신설(schema+prompt). '이 사건이 감사인 확인·후속절차 요하는가' — LLM 판단, '계정 붙으면 중요' 코드규칙 아님(원칙 1), 8번째 축 아님·severity_rank 불변(원칙 8, 주체성과 동일 패턴). default_visible 이 이 값 읽어 방향 중립이어도 노출(2300억 매각·세일앤리스백·10조 우발채무류 접힘 공백 보완 — total-verify #2). ★과다노출 방지: 프롬프트가 '실제 후속확인 필요 사건에만 true, 단순 동향·가격조정·홍보는 false' 서술(고정목록 없음). 라벨 '감사 확인 요망'+2층 설명. [C-2 방향 정밀화] 방향=재무제표 영향(원칙 6) 강화: 같은 실제 사건은 같은 방향(진행단계가 방향 안 뒤집음 — 단계는 stage 축), 확정 중립 vs 검토 호재 흔들림·매각 중립 vs 개선 호재 흔들림 교정. [C-3 악재 과잉] 홍보·기획 기사의 배경 부진 언급 ≠ 악재 사건(뉴 웨이브 기획기사가 '실적 부진 대응' 한 구절로 악재 상단 오른 것 교정). [C-4a] 담당 계정 미선택이면 '남의 계정(③)' 성립 안 함 → 계정 붙은 뉴스도 ② 공용에, ③은 비고 라벨 '선택 전' 표기(ranking.classify_and_rank). [C-4b] 접힘 뷰 항목도 주체성 라벨 일관(루프10에서 접힘도 동일 rowHTML 사용 — 이미 적용, 실측 확인). 고친 파일: schema.py(audit_attention 필드·스키마·parse_batch·하위호환), prompts/system.txt(방향 정밀화+감사중요 블록), serialize.py(default_visible+judgment_view+load_full 하위호환), ranking/__init__.py(no-viewer 라우팅), index.html(감사 확인 요망 뱃지·2층·CSS). 실측(무API): audit중립 노출·단순중립 접힘(과다노출 0)·기권/무관은 audit 무시(방어선 우선)·선택전 ② 공용·③ 빈·접힘 C 라벨. tests/unit/test_audit_attention_and_routing.py 9건 추가. ■ 정직(비용): 판단 프롬프트 3곳 변경으로 기존 판단캐시 무효화. 라이브 분류·노출건수(감사중요 true 비율·중복 묶임 실제)는 크레딧 없어 미실행 — '실행 못 함' 정직 보고(억지 통과 주장 안 함). 표시·BS 메커니즘은 결정론적 실증. 전체 unit 48 pass, 비-API 불변식(routing·conservation·subject_role_demotion) 24 pass, 전체 수집 97 무에러. 유지: 수집·깔때기·주체성 3분류·증발방어·계정 울타리·근거강제·접힘≠삭제·그룹선택(루프8)·계정 매칭 로직 불변. pending: total-verify #2·loop7-bs-fallback #1 반영 표기, #3·#5·재현성은 잔류. 검증방 몫(원칙 10): BS 섹션(미확정 포함) 불변식·감사중요 표시 불변식은 검증방이 tests/invariants/. decision-log D30.

- 루프10 / 2026-07-18 / (판단층에 관점 1개 추가 + 표시 강등, 기존 7축·수집·깔때기·계정매칭·라우팅배정·증발방어 불변) 주체성 3분류 — '이 기사가 이 회사를 다루는가'. 근거: 실전 발견(총괄검증 pending total-verify-observations #4 '비교 언급 오염'을 설계자가 '넣어라'로 결정) — "삼성전자 2Q 영업익…" 주간 브리핑에 CJ 과징금 한 줄이 섞여 CJ 악재 상단에 뜸(제목≠내용→신뢰 붕괴). 단 제목 일치로 자르면 "CJ씨푸드 관리종목"(자회사·제목에 모회사명 없음=중요)·"건설사 부채비율…태영489%"(복수사 나열이나 태영 수치가 실질=중요)를 잃음. ■ 핵심 설계: 주체성은 8번째 심각도 축이 아니다(원칙 8) — 판단층은 A(단독주체)/B(복수주체)/C(타사언급)를 '값'으로만 표기하고 severity_rank(7축)는 불변, 표시층이 그 값을 '읽어' C만 하단으로 강등(default_visible 이 direction 읽는 것과 동일 패턴, 원칙 1·5). ■ 고친 파일: src/judge/schema.py(Judgment.subject_role·subject_role_reason 필드 기본값 ""[미분류→강등 안 함], 상수 SUBJECT_SOLE/MULTI/PERIPHERAL + is_peripheral_mention 술어['명백한 C'에서만 True], _ITEM_SCHEMA 에 subject_role enum·reason 필수 추가, parse_batch 가 .get 으로 실어나름[구 dict 하위호환]), src/judge/prompts/system.txt(주체성 3분류 관점 블록 — ★제목 포함으로 기계적으로 가르지 마라·★고정 키워드/패턴 목록 금지[원칙1]·★애매하면 C 아니라 B[놓침 방지 원칙7]·C면 기사 주체 누구·어떻게 언급됐는지 근거 동반[원칙3]; 기존 7축 지시는 무변경, severity_rank 는 주체성으로 올리거나 내리지 말라 명시), src/ranking/__init__.py(rank_by_severity 는 순수 심각도로 유지 + demote_peripheral[C만 안정적으로 뒤로, A/B 상대순서·severity_rank 불변] + rank_for_display[심각도순 위에 강등을 얹은 단일 표시 진입점], classify_and_rank 바구니 내부정렬을 rank_for_display 로[배정=라우팅은 불변, 순서만 C 하단]), src/api/server.py(_split_visible 이 rank_for_display 사용 — C 악재는 접힘 아니라 표시분 하단), src/api/serialize.py(judgment_view 에 peripheral_mention·subject_note 노출, _judgment_from_dict 하위호환 .get), src/api/index.html(C 라벨 뱃지 '타사 기사 내 언급'[b-periph], 2층에 '이 기사 주체 누구·어떻게 언급' 설명, 표시분 C 경계에 구분선). ■ 실측(무API — 표시층만 구동, LLM 값 A/B/C 주어졌다고 가정): 루프 예시 그대로 모사 — B("부채비율…489%")·A("압수수색")·B(자회사, 제목에 모회사명 없음) 상단 유지, C("타사 브리핑 속 과징금", 심각도 95로 최고)가 A/B(심각도 70·60·55) 아래로 강등(주체성이 심각도 이김), peripheral_mention=True·2층 설명 노출, folded_view 에 없음(삭제·접힘 아니라 표시분 하단·스크롤 접근), 제목 미포함이라도 B는 상단·C는 하단(배치는 제목 문자열 아니라 subject_role 값만 읽음), 계정 바구니는 배정 불변·순서만 C 하단. 옛 예시 덤프(주체성 필드 없음)→미분류→강등 안 함(삼성 표시2·접힘28 회귀 0). ■ 정직: LLM '분류 정확도'(실제로 태영489%를 B로, 삼성 브리핑 CJ를 C로 다는지)는 라이브 필요 — 이 환경에 ANTHROPIC_API_KEY 없음 + 프롬프트 변경으로 기존 판단캐시 115개 전량 무효(구 프롬프트 키) → 라이브 재실행은 실비용, 크레딧 주의로 '실행 못 함'(억지 통과 주장 안 함). 표시 메커니즘은 결정론적으로 실증됨. tests/unit/test_subject_role_display.py 9건 추가, 전체 unit 35 pass, 표시·라우팅 관련 비-API 불변식(test_routing·test_survivor_judgment_conservation) 10 pass(회귀 0). pending: total-verify-observations #4 를 '개발 반영(루프10)'으로 표기(설계자 정책 결정 시공) — 나머지 #1·#2·#3·#5 는 설계자/검증방 몫으로 잔류. 검증방 몫(원칙 10): 주체성 표시강등 불변식(명백한 C만 강등·애매/미분류 불변·severity_rank·바구니배정 불변·C 접힘 아님)은 검증방이 tests/invariants/ 에 세운다. decision-log D29.

- 루프9 / 2026-07-18 / (blocker — 증발 방어, 판단 7축 불변) 배치 반환 대조 + 누락 '미판단' 보존. 총괄검증 발견: Opus 판단 배치가 입력 survivor 보다 적게 반환하면(예 206→205) 코드가 검출 못 해 뉴스가 조용히 증발(원칙 7·4 침묵 위반, 간헐적이라 델타검증 못 잡음). 고친 파일: src/judge/engine.py(judge_items 반환 직전 _reconcile_returns 로 '입력 items 전부에 정확히 1 Judgment' 강제 — 누락→make_missing_judgment 강제기권 보존, 중복→첫 것만, 유령→채택 안 함, 입력순 재조립·불일치 stderr 경고), src/judge/schema.py(Judgment.unjudged 플래그 + make_missing_judgment: abstained/unjudged=True·근거/계정/규모 지어냄 없음·⚠ 이유), src/pipeline.py(funnel 에 survivors·judged·unjudged 노출), src/api/serialize.py(fold_reason '⚠ 미판단…'·judgment_view 에 unjudged·load_full 하위호환 .get), src/api/server.py(resp_funnel survivors·unjudged, counts.unjudged), src/api/index.html(상태뱃지 '⚠ 미판단'·깔때기줄 '⚠ 미판단 K건'). 판단(7축)·프롬프트·모델·thinking·캐시·payload 무변경 — '반환 집합 보존'만 강제. 청크 예외는 여전히 그대로 전파(백필은 '성공했으나 일부 누락'에만), test_parallel_invariance(병렬==순차·실패전파) 2건 통과 유지. 정합: judged==survivors 항상 성립(증발 0), '수집 = 표시 + 접힘'(접힘에 미판단 K 포함) 항등식 복원. 실증(무API 몽키패치 배치 부분반환): 5중 4반환→누락 1 unjudged 보존(근거0·계정0), 중복(1×2)+유령(99)+드롭(2)→출력 정확히 [0,1,2,3], 서버 응답 수집7=표시4+접힘3·survivors5==judged5·미판단 접힘뷰 존재. 옛 예시 덤프도 복원 OK. tests/unit/test_reconcile_no_evaporation.py 3건 추가, 전체 unit 26 pass. pending: total-verify-survivor-judgment-conservation 에 '개발 반영(#2·#3)' 추가하되 처방 #1 불변식(tests/invariants/)은 검증방 몫으로 잔류(원칙 10 — 개발방은 불변식 못 만듦). decision-log D28.

- 루프8 / 2026-07-18 / (표시·선택 UX만, 판단·수집·계정매칭·라우팅 불변) 합계 계정 = 그룹 전체 선택 버튼. 루프7(D25)에서 회색 잠금이던 합계 계정(자산총계·유동자산·부채총계 등)을 클릭하면 그 그룹의 '말단 계정 전부'가 한 번에 선택된다(중간 합계 말고 말단만 — ★중첩). 소속은 이름 고정표가 아니라 루프7 산술 판별을 재활용(원칙1): (1) 섹션 총계(자산·부채·자본총계=DART IFRS 앵커ID)는 _assign_sections 앵커 배정을 재활용해 '그 섹션 말단 전부'(총계가 앞/중간 어디 있든 견고), (2) 중간 합계(유동자산·비유동자산·지배지분 등)는 _pass1_subtotals(=_detect_subtotals 의 pass1 전위재귀를 공유 헬퍼로 추출)의 서브트리에서 '말단만'(그 안 중간 합계는 제외). 소속이 산술로 안 잡히는 합계(pass1 서브트리 없는 비앵커)는 members 미부착 → 기존처럼 잠금(지어내지 않음, 원칙4). 그룹 선택도 '선택'은 말단 체크박스로만(합계 자체는 바구니에 안 담김) → 그룹선택 결과=말단 개별선택 결과(같은 selected 리스트→같은 classify_and_rank), 재클릭 토글 해제. 고친 파일: src/sources/dart.py(_pass1_subtotals 추출[_detect_subtotals 출력 불변]·_subtotal_members 신설·_build_bs_table 이 합계에 members 부착), src/api/index.html(합계=초록 '그룹' 버튼 렌더·toggleGroup·refreshGroupStates·CSS). serialize·server·pipeline·판단 4모듈·라우팅 무변경. 리팩터 회귀 0: 캐시 4사 골든 대조로 is_sub·fallback_used·(name,selectable) 바이트 동일. 실증(무API 캐시 4사): 한화 자산총계→자산 말단 21개(유동10+비유동11, 중간합계 제외)·부채총계 18·자본총계 7, 삼성 자본금=보통주+우선주 3단 중첩까지 정확(지배지분 클릭 시 중간합계 자본금 빠지고 말단만), 회사마다 합계명 상이('지배기업 소유주지분' vs '지배기업의 소유주에게 귀속되는 자본')에도 산술 동작=이름표 없음 방증. 라우팅 동일성 실측(한화): 자산총계 그룹선택 selected==개별선택 selected·바구니 크기 동일·그룹선택이 자산 관련 뉴스를 ①로 끌어옴. 단위테스트 tests/unit/test_bs_subtotal_members.py 4건 추가(캐시 게이트 skip), 전체 unit 23 pass. pending: 확인했으나 전부 판단·수집·검증 계층(무관) — 이번 표시-only 스코프 밖이라 미반영, 그대로 둠(설계자 '무관하면 그대로' 지시 + 원칙10 검증방 소유 불변식 존중). decision-log D27.

- 루프7 / 2026-07-17 / (성능·표시만, 판단 7축 불변) 병렬 처리 + 화면 다이어트 + 발행일 + 재무상태표 T계정 표. 서브에이전트 2개 병렬(파일 무겹침): A=백엔드 병렬(engine.py·prescreen.py), B=프론트/데이터(dart.py·serialize.py·server.py·index.html). [A] 판단 병렬화: judge_items 청크 루프 + prescreen 1패스 배치 루프를 ThreadPoolExecutor(동시 상한 6)로 동시화, anthropic 클라 max_retries=5(429 백오프), 실패 청크 예외 전파(유실 없음, 원칙4), on_usage Lock 직렬화. 결과 불변 보장: 각 청크 독립 + 캐시키(모델·system·payload)로 순서·스레드 무관 + 원래 인덱스로 재조립 → 병렬 출력=순차 바이트 동일. prescreen 2패스(전역군집)는 단일콜 순차 유지. 판단 로직·프롬프트·스키마·모델·thinking·캐시 무변경(오케스트레이션만). [B-1] 카드 다이어트: 1층 뱃지=상태(악재/호재/위험해소/기권/중립)+계정만(규모 뱃지 제거, 규모는 판단·정렬엔 유지), 2층=방향 근거문장 1개 + 계정가설(이유+근거, 계정 중복제거)만(방향문장·규모문장 제거). 계정 근거는 유지(AI 추론 검증 필요, 원칙3). [B-2] 발행일: 네이버 pubDate→YYYY-MM-DD, 못 읽으면 '날짜 미상'(지어내지 않음, 원칙4). [B-3] 계정선택=재무상태표(BS) T계정 표(자산 좌·부채 우상·자본 우하, 계정 이름만·금액 전부 제거, BS만-손익/자본변동/현금흐름 제외). 1단계 확인(4사 실측): 자산/부채/자본 대분류는 DART 자체 IFRS 앵커 계정ID로 존재하나 합계vs말단 계층 필드는 없음(account_detail 전부 '-'). 합계 판별은 이름표 아니라 금액 산술구조로(_detect_subtotals: 합계=자식 합, 전위재귀+고정점, 메모이즈로 지수폭발 방지-순수함수라 결과 동일). 섹션은 IFRS 앵커ID(우리 분류표 아님, sj_div 성격·D22)+금액 포함관계로 배정, 총계(자본과부채총계)는 어느 섹션도 아님, 못닫으면 폴백+fallback_used 정직표기(지어내지 않음). 말단만 선택가능, 선택→확인→표 접힘+선택 계정만 표시+그 계정 뉴스 '내 계정' 바구니(기존 route, 판단 재실행 없음). 판단 울타리는 여전히 flat 전체(불변). 금액은 탐지 내부용, 화면엔 이름만(원칙5). dart.py 가 fetch_account_statements 에 합성 그룹 sj_div=='BS_TTABLE' 로 T표를 실어 pipeline·server 무변경 통과, serialize.bs_table_from_statements 로 복원(벤더 직접 import 없음, 원칙2). 실증(무API 4사): 한화오션 자산 21·부채 18·자본 7 선택가능(합계 자산총계·유동자산·부채총계·비유동부채 등 정확 제외), 삼성 자본금=보통주+우선주 중첩 합계까지 정확 탐지. bs_table 페이로드 키=name·selectable(금액 미노출 확인). 태영 라이브: 수집1056→중복750 접힘→간이무관187 접힘→판단119(표시 28), 정합 1056=28+91+937 OK, BS T표 라이브 생성(자산 19·부채 15·자본 5 선택가능). 성능 실측: 병렬 태영 웹 실행 270초(약 4.5분). 정직: '1분 내' 목표 미달 — Opus adaptive thinking 청크당 지연(~1~2분)이 상한. 순차 동일 워크로드는 각 LLM콜 지연 합산으로 약 13~15분 추정(별도 순차 측정은 비용·시간상 생략), 병렬이 동시6으로 겹쳐 ~3~4배 단축. 더 줄이려면 thinking 완화/청크·동시 확대 필요(판단 품질 트레이드오프-스코프 밖). 전체 테스트 46 pass(검증방 신규 test_prescreen 5건 포함, 병렬 경로서 통과=결과 불변 방증). pending 반영: loop6b-naver-ceiling 옵션(a) 문구 정직화(routing-rules), (b) 보류. decision-log D24·D25·D26, challenges C13b·C14 추가.

- 루프6b / 2026-07-17 / (수집·입구 깔때기 신설, 판단 7축 불변) 수집 제한 제거 + 넓은 1년 수집 실제 반영. 진단: 경우 B(백엔드 미구현) — fetch(display=30) 단일요청 + run 에 중복제거·Haiku 스크리닝·3단 깔때기 부재(루프6 검증방이 지적한 전제 불일치와 일치). 수정: (1) 수집건수 드롭다운(15/30/50) 제거 — 사용자 미선택, 시스템이 최근 ~1년치 자동 넓게 수집(NaverNewsSource.fetch_broad: 회사명 단일검색어·정확도순+최신순·페이지네이션·1년필터·동일URL literal중복만 제거). 네이버 날짜범위 파라미터 부재로 '기간창 분할'은 최신순 페이지네이션+클라 필터로 근사, 1000건 상한 도달은 화면 표기(원칙7). (2) 판단 앞 깔때기 신설 src/judge/prescreen.py(Haiku, 2패스: 배치별 관련성+군집 → 대표만 모아 전역 재군집). 중복 정리(같은 사건 대표 1개)+간이 관련성 관문(명백 무관만 접음, 느슨-애매하면 통과). 같은 사건인지/무관인지는 Haiku 이해로(event_id·relevant), 코드는 묶기만 — 키워드·문자열 유사도 임계 없음(원칙1). 병합 보수적(애매하면 안 합침-다른 사건 병합=증발 금지, 과분할은 허용). (3) 접힘=삭제 아님(원칙7): 중복·간이무관 전부 prejudge_folded 로 보존, 화면 하단 접근 가능, 경량 렌더(screened_out). 수집=표시+접힘(판단후+입구) 정합. (4) UI: 드롭다운 제거, '수집N·표시M·접힘K' + 깔때기 흐름줄(수집→중복정리후→정밀판단), 1000상한 표기. server/pipeline 이 RunResult(items·judgments·prejudge_folded·funnel) 반환. 판단(judge/schema/prompt/ranking) 로직 불변 — engine 에 비용측정용 텔레메트리 on_usage(입출력 무영향)만 추가. 기술: Haiku4.5 effort 미지원(400)→format만, 대용량 출력 SDK 스트리밍 요구→messages.stream, 잘린 JSON 캐시 금지(파싱 후 캐시). 실증(태영건설 라이브): 수집 1056(네이버 date1000 상한도달+sim300, URL중복244 제거) → 중복766 접힘→distinct290 → 간이무관142 접힘 → 풀판단 148(Opus, 기본화면 악재 34·판단후접힘 114). 정합 1056=34+114+908 OK(증발 0). 워크아웃/PF 도배가 대표로 접히고 새 악재 표면화: 계열사 채무 138억 주식 담보제공(특수관계자·우발채무)·부산 정비사업 계약해지(매출2.7%)·반포 PF 공매·하도급 지급여력·사용자성(노무 우발채무)·철근가 원가압박·부채비율489%·지배구조 준수율46%. 비용 실측: Haiku 콜드 10콜 225K/25K=$0.35, Opus 10배치 147K/92K(thinking 포함)=$3.03, 합계 ~$3.38 — Opus 전량(1056) 대비 약 6배+ 압축. 전체 테스트 41 pass. pending 반영: loop6-screening-premise-mismatch(설계 결정 '도입' 반영, 검증방 재검증 이관)·loop6-dedup-must-not-merge(dedup 도입-가드 발효, 검증방 앞단 불변식 확장 요망). docs/routing-rules.md(수집·입구 깔때기), decision-log D23, challenges C13, out/results/loop6b_report.md.

- 루프6(검증) / 2026-07-17 / [검증방] 스크리닝 거짓 탈락·하드코딩 검증. 역검증 발견: 지시가 가정한 '스크리닝 게이트(예/아니오/모름)'와 '중복 제거'가 코드에 없음 — 관련성은 7축 판단 안의 relevant 필드, 접힘은 표시필터 default_visible(루프5), dedup 부재(grep·run 확인). 실재 메커니즘(LLM 관련성 판단 + 표시필터)을 대상으로 실측. 검증1(거짓 탈락) 통과: 가상 명백악재 6종(워크아웃·분식정황·대규모소송·감사의견거절·횡령·디폴트) 전부 relevant=True·악재·기본화면 유지, 경계 2종(파업·신용전망) 무관탈락 없이 통과, 순수무관 2종(봉사·광고) relevant=False→'무관' 접힘(삭제 아님·보존), 위험해소(무혐의)→stage 무혐의/취하 위험해소로 유지, 부산/울산 화재 별개 2건 보존(병합 없음), 입력 13=판단 13(증발 없음). 검증2(하드코딩) 통과: 프롬프트·default_visible 에 사건유형/키워드/점수표 없음('분식 의혹 혐의없음'을 '분식' 단어에도 위험해소로 판단=이해 기반 방증). 검증3 통과: 수집 회사명 단일 검색어(부정 키워드 없음), dedup 없음(오병합 위험 없음), 판단 4모듈(judge/schema/prompt/ranking) 루프5·5b서 미변경(입구만 표시 바뀜), collected=shown+folded 정합(증발 없음). 검증4: 루프5·5b 개발방 자율판단 원칙 합치(위험해소=stage 판단값 읽기·표 그룹화=DART sj_div 원본 — 하드코딩 아님). 신규 불변식 tests/invariants/test_screening.py(5건, 거짓탈락·위험해소·경계·무관보존·미병합) 추가 — 검증방 소유. pending: loop6-screening-premise-mismatch(설계자 판단 요청), loop6-dedup-must-not-merge(dedup 도입 시 가드). 전체 테스트 41 pass.
- 루프5b / 2026-07-17 / (표시 계층만, 판단 불변) 계정 선택을 재무제표 표 형태로 + 접기. 1단계 확인: DART fnlttSinglAcntAll 응답의 각 행이 재무제표 구분(sj_div: BS/IS/CIS/CF/SCE, sj_nm)과 순서를 이미 보유(삼성 213행·한화오션 모두 5개 표) — 현 어댑터가 flat 추출로 버렸을 뿐. 2단계: FinancialSource.fetch_account_statements 추가(구분 없는 소스는 base 기본=단일 그룹, DartSource override=_group_by_statement). sj_div 첫 등장 순서로 표를 놓고 표 안은 응답 행 순서 그대로(ord 불신 — 표마다 1부터 아님·SCE 동일값) 계정 중복 제거. 구분·순서를 DART 원본대로만(임의 분류 없음 — 단위테스트로 '그룹화 집합==flat 울타리 집합' 못박음). run() 5-튜플로 statements 반환(pipeline.main 언팩 갱신), api 응답에 account_statements+linked_accounts, full_dump/load_full 이 statements 저장·복원(하위호환 기본 []). 3단계 UI: 흩뿌린 체크박스→재무제표별 접이식 표(계정 이름만, 금액 미표시), '내 계정 선택' 패널 전체 접기(선택 적용 후 자동 접힘), 뉴스카드 2층 토글을 양방향 확실화(toggleItem: classList.toggle + 캐럿 자세히▾/접기▴, 제목 링크는 stopPropagation). 계정 선택은 여전히 표시 렌즈 — 판단(심각도·방향·근거·계정가설) 불변, 담기는 바구니만. 판단 로직(judge/ranking/schema/prompt) 무변경, src→reference import 없음. 예시 재저장(account_statements 추가, 오프라인·캐시만 — 토큰 0): out/results/{hanwha_ocean,samsung}.json. 실증(실소켓): 한화오션 표 5개(재무상태표55·손익17·포괄손익10·현금흐름57·자본변동14), BS='자산총계/유동자산/…' DART 순서, route 표시분 재배치 동작. 전체 테스트 36 pass(+account_statements 5). decision-log D22, challenges C12, docs/account-linkage.md(표시용 표 주석) 추가.
- 루프5 / 2026-07-17 / (표시 로직 + 디자인, 판단 불변) 기본 화면 필터·접힘·UI 개편. 판단 로직(judge/ranking/schema/prompt) 무변경 — 표시 계층(api/)만. ①기본 화면 표시 규칙: 재무·감사 악재 + 위험 해소만 노출, 단순 호재·무관·기권은 하단 접이식으로 접음(serialize.default_visible/fold_reason). '위험 해소 vs 단순 호재'는 새 키워드표가 아니라 기존 판단값으로 가름 — 악재=direction=='악재', 위험해소=stage=='무혐의/취하'(프롬프트·severity-axes 가 리스크 해소를 그렇게 인코딩), 무관=relevant, 기권=abstained(원칙 1·5·8). ②접힘=삭제 아님(원칙 7): 접힌 뉴스는 데이터 보존, 하단 "표시되지 않은 뉴스 N건 ▸"에서 이유 라벨과 함께 전부 펼침, 수집·표시·접힘 카운트 노출(투명성). 위험 미배정 악재는 기본 화면 ②에 잔류(숨기지 않음). ③뱃지/펼침 중복 제거(뱃지=상태만, 2층=위에 없는 이유만) + 2층 다이어트(근거문장·규모·방향·계정가설 4가지). ④디자인: 어두운→밝은 카드형 라이트 테마, 여백·정보계층화, 한국어, 벤더명(OpenDART 등)은 키입력 안내에만(본문 비노출, 원칙 5). server 응답에 counts{collected,shown,folded}+folded_view 추가, 바구니엔 표시분만 배치(route 도 표시분 재배치). (loop4 처방 반영) 예시 덤프 sanitize: full_dump 가 심각도순 정렬 후 severity_rank 를 위치 서수(N..1)로 덮어써 커밋 파일에 내부 원값 미노출(load_full 로 순서 재현 유지) → out/results/{hanwha_ocean,samsung}.json 재저장(둘 다 1..N 순열 확인). 실증(표시-only라 새 LLM 호출 없이 루프4 실제 판단을 새 표시 로직으로 렌더, 실소켓 확인): 한화오션 수집40·표시1·접힘39(8000억 수주는 단순호재로 접히되 매출 대비 7.4% 근거 온전), 삼성전자 수집30·표시2·접힘28. 관측: 현재 계정연결 뉴스가 대부분 수주 호재라 접혀 계정별 'mine' 이 비는 정직한 상호작용. 전체 테스트 31 pass(+display_filter 7). pending: loop4-example-dump 의 덤프 처방 비움(‐KEYS 스레드 관측만 잔류), fake-precision·loop2-2·3·4 는 스코프 밖 그대로. decision-log D20·D21, challenges C11, docs/routing-rules.md(기본 화면 표시 규칙) 추가.
- 루프4 / 2026-07-17 / (신규 기능) 웹 화면 + 원클릭 실행: 로컬 Flask 서버(src/api/server.py, 127.0.0.1:8765) + 단일 HTML 화면(src/api/index.html, 한국어·프레임워크 없음) + 판단→화면 직렬화(src/api/serialize.py) 추가. 기존 코어(pipeline/judge/ranking/sources)를 '호출만' 하고 판단을 재구현하지 않음(원칙 9 준수 — reference import 없음). 화면이 판단을 왜곡하지 않는 구조: /api/analyze 가 판단을 1회 내려 불변 리스트로 서버 메모리에 캐시, /api/route 는 판단 없이 classify_and_rank 로 '재배치만'(표시 렌즈) — 계정 선택이 심각도·방향·계정가설·근거를 안 바꿈(실증: 선택 달라도 1층 판단 전건 동일=True). severity_rank 등 내부값은 화면 dict 에서 제외(원칙 5, 순위는 '위치'로). 키 4개는 화면입력→서버 메모리(_KEYS)에만, 실행 직전 os.environ 주입, 파일·로그 기록 없음, 에러·로그는 _redact_secrets 로 crtfc_key·키값 가림. 다중 계정 선택 위해 classify_and_rank 를 str|Iterable[str]|None 로 하위호환 확장(검증방 test_routing 문자열 경로 그대로 통과, 개발방 단위테스트는 tests/unit/test_routing_multi.py). 원클릭 실행 start_news_risk_monitor.bat/.sh(더블클릭→서버→브라우저 자동), requirements.txt, README.md(한국어). 예시(키 없이 보기): out/results/{hanwha_ocean,samsung}.json 에 실제 실행 결과 저장, full_dump/load_full 로 라이브와 동일 코드로 복원(예시 파일에 키 없음 확인). .gitignore 에 out/results 커밋 예외 추가. 실증(라이브, 실소켓+test_client): 한화오션 40건 — 태국 호위함 8000억 수주→매출 대비 약 7.4%(코드 계산), 계열분리→미상 유지, 계정연결 2건 '내 계정'으로, 38건 ② 공용큐. 삼성전자 30건 전부 노이즈→계정0·비율0 정직한 미배정/미상. 전체 테스트 24 pass. pending fake-precision-breadth 는 스코프 밖이라 미반영(그대로 둠). decision-log D18·D19, challenges C10 추가.
- 보안패치 / 2026-07-17 / 키·캐시 git 노출 차단(예방): .gitignore 신설(.env·.cache/·*.zip·scratchpad/·__pycache__/·.pytest_cache/ 제외, .env.example·out/.gitkeep 은 추적 유지) + .env.example 신설(키 '이름'만, 값 없음). 현재 저장소가 git 미초기화 상태라 추적 중 파일이 없어 git rm --cached 는 불필요(무해). 격리 저장소에서 check-ignore 로 규칙 검증(민감경로 전부 무시·소스/문서 전부 추적 확인). 키 값은 어디에도 출력 안 함. 근거: 검증방이 루프1~3 반복 지적한 평문 키·캐시 트리 노출.

- 소급패치 / 2026-07-16 / 용어 정리: '딜' 제거, 딜성 사건은 감사 영향 관점으로 축 1에 유지.
- 소급패치 / 2026-07-16 / decision-log D2/D6/D8 표현 정정(버림→발전, '부정'→'부정적 뉴스' 명확화), challenges.md 작성.
- 루프1 / 2026-07-17 / triage 판단 로직 구현: NewsSource 인터페이스+네이버 어댑터(중립 수집), 판단 계층(배치 LLM 호출·구조화 출력·docs 원칙 주입), schema.py 확정, 심각도 정렬(단일 리스트). 제외: 계정연결·3바구니 라우팅·DART·본문 크롤링·웹 UI. decision-log D11 / challenges C7 추가.
- 루프1-보강 / 2026-07-17 / 키 배선 수정: 원인은 코드가 os.environ만 읽는데 키는 프로젝트 루트 .env에 있어 아무도 로드하지 않음. 수정은 pipeline 진입점에 무의존 최소 .env 로더 추가(python-dotenv 미설치, override=False로 셸 환경 우선, 값은 코드에 안 심음). 실제 뉴스 전 구간 첫 성공(삼성전자 15건 수집→LLM 판단→정렬, exit 0). decision-log D12 추가.
- 루프3 / 2026-07-17 / (pending 반영) P1 evidence 공백 우회 차단: _enforce_evidence 를 len==0 → any(quote.strip())(실질 근거 유무)로, 계정 울타리와 기준 통일 → 검증방 빨강 test_evidence_defense.py::blank_quote_bypass 초록화. P2 테스트 폴더 분리: 개발방 저작 test_account_fence·test_evidence_required 를 tests/invariants/ → tests/unit/test_schema_defenses.py 로 이동(원칙 10, invariants는 검증방 전용). P3 근거 추적성: 코드 가드 미도입(정직한 발췌 오판 방지), 검증방 quote_traceable canary 에 위임(관측). (신규 기능) 규모 DART 앵커: FinancialSource.fetch_financials(매출·자산총계·자기자본, 표준 IFRS 계정ID 추출), 분모는 LLM이 사건 이해로 선택(고정표 없음)+근거, 분자는 명확한 단일 금액일 때만(범위·모호는 미상 유지, 가짜정밀 금지), 비율(분자÷분모)은 코드가 결정론적으로 계산(apply_magnitude_anchor). 계정목록·재무숫자를 같은 재무제표 응답에서 뽑아 중복호출 방지(캐시 statement_*.json). 실증: 삼성전자 30건 — (a)1500억 출연→'매출액 대비 약 0.05%' 코드 계산, (b)용인 팹 대규모 설비투자→금액 불명이라 '미상' 유지(비율 미계산). pending: P1·P3 비움, P2는 관측 2·3·4만 잔류. decision-log D16·D17, challenges C9 추가.
- 루프2 / 2026-07-17 / (pending 반영) ①evidence 강제: 근거 없는 비-기권 실질 판단을 parse_batch에서 강제 기권으로 강등(폐기 아닌 강등=원칙 7, 근거 날조 없음). 스키마 minItems 대신 코드 조건부 집행(기권 항목에 근거 강요 방지). ②magnitude 'size=해당없음' 상태 추가 — 무관 뉴스에 '미상(조사 중)' 경보 오용 방지(prompt·render·docs/magnitude-rules 동기화). (신규 기능) 계정 연결: DartSource 최초 구현(OPENDART, 계정과목 '목록'만·재무숫자 아님), 계정 후보를 울타리로 LLM에 주입, LLM은 가설+근거로 연결, enforce_account_fence가 후보 밖 계정·무근거 연결 제거. 3바구니 라우팅 classify_and_rank(내계정/미배정/타계정, 바구니 내 심각도정렬, 하드필터 없음, 미배정 안 숨김). 판단·계정·corpCode 캐시(.cache/). 불변식 test_evidence_required·test_account_fence(비-API 방어선). 실증: 삼성전자 15건 전 구간 성공(계정후보 121개, 계정연결·잦은 계정기권 11~12/15). pending 2건 비움. decision-log D13·D14·D15, challenges C8 추가.
